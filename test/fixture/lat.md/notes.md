# Notes

Fixture notes covering aliased, mixed-case and path refs.

## Refs

An aliased ref to [[widget#Rendering|the rendering pass]].

A mixed-case ref to [[Widget#Rendering]].

The panel method is [[src/widget.ts#Panel#render]] and the standalone one is [[src/widget.ts#render]].

## Directories

The [[tool]] directory has an entry file; the [[src]] directory has none.

## Dangling

A ref to [[does-not-exist]] resolves to nothing.
